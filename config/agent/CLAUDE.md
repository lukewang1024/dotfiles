# Claude Code reads CLAUDE.md, not AGENTS.md — so import the shared, tool-agnostic
# conventions explicitly. Single source of truth lives in AGENTS.md (symlinked to
# the codex/opencode global paths); this file just pulls it in for Claude.
@~/.claude/AGENTS.md

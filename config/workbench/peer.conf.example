# Copy to $XDG_CONFIG_HOME/distributed-workbench/peer.conf.
# Keep the destination private and do not commit its values.
DISTRIBUTED_WORKBENCH_PEER_HOST=ssh-alias
DISTRIBUTED_WORKBENCH_NODE_ID=this-node
DISTRIBUTED_WORKBENCH_VERSION=latest

# show status bar at bottom for remote session,
# so it do not stack together with local session's one
set -g status-position bottom

# use hostname (@#h) in set-titles-string with SSH
set -g set-titles-string "[tmux] [#S:#I.#P @#h] #W"

# The SSH client already has a clock and its battery is not the remote host's.
# Keep only remote CPU above 100 columns, then preserve Host + Agent at every
# narrower width. Continuum remains active through @adaptive_maintenance.
set -g @adaptive_time_min_width 9999
set -g @adaptive_date_min_width 9999
set -g @adaptive_battery_min_width 9999
set -g @adaptive_cpu_min_width 100

# Each window sizes to the smallest client that currently has it focused, so a
# phone and a desktop attached to the same (or a grouped) session don't force
# every window down to the phone's size — only the window the phone is looking at.
setw -g aggressive-resize on

# syntax:ssHash
